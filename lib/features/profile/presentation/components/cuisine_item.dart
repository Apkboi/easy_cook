import 'package:flutter/material.dart';
class CuisineItem extends StatelessWidget {
  const CuisineItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card();
  }
}
